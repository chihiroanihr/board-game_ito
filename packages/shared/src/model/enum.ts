// Enum for player's status
export enum UserStatusEnum {
  IDLE = 'idle',
  PENDING = 'pending',
  PLAYING = 'playing',
}

// Enum for room status
export enum RoomStatusEnum {
  VOID = 'void',
  AVAILABLE = 'available',
  FULL = 'full',
  PLAYING = 'playing',
}
