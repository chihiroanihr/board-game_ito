{
  "name": "board-game_ito",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "Rhina Kim",
  "license": "ISC",
  "keywords": [],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install-packages": "npm install --legacy-peer-deps",
    "client": "npm --prefix packages/client start --host=192.168.1.205:3000",
    "server": "npm --prefix packages/server start",
    "server-dev": "npm --prefix packages/server run dev",
    "dev": "concurrently \"npm run server-dev\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  },
  "dependencies": {
    "install-packages": "^0.2.5"
  }
}
